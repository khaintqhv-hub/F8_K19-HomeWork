services:

  react-app:

    container_name: react-container

    build:
      context: .

    ports:
      - "5173:5173"

    volumes:
      - .:/app
      - /app/node_modules

    stdin_open: true

    tty: true